import PageLoader from "@/components/PageLoader";

// Attente affichée à chaque changement de page (fallback par défaut de toute l'app).
export default function Loading() {
  return <PageLoader />;
}
